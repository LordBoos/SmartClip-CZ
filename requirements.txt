# Core dependencies for SmartClip CZ
numpy>=1.19.0
scipy>=1.5.0
scikit-learn>=0.24.0
requests>=2.25.0

# Audio processing
sounddevice>=0.4.0
librosa>=0.8.0

# Speech recognition
vosk>=0.3.30

# Advanced emotion detection (now mandatory)
opensmile>=2.0.0

# GUI dependencies (for confidence widgets)
# tkinter is usually included with Python
# PyQt5 or PyQt6 may be needed for advanced widgets (install separately if needed)
